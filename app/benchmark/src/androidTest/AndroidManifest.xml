<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.sygic.samples.benchmark">

    <!--
      Important: disable debugging for accurate performance results

      In a com.android.library project, this flag must be disabled from this
      manifest, as it is not possible to override this flag from Gradle.
    -->
    <application
            android:debuggable="false"
            tools:ignore="HardcodedDebugMode"
            tools:replace="android:debuggable">
        <activity
                android:name="com.sygic.samples.benchmark.BenchmarkActivity"
                android:theme="@style/Theme.AppCompat"/>

        <meta-data
                android:name="@string/com_sygic_api_key"
                android:value="${sygicApiKey}"/>

    </application>

</manifest>