<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sygic.samples">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activities.SamplesActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BrowseMapDefaultActivity"
            android:label="@string/browse_map_default" />
        <activity
            android:name=".BrowseMapFullActivity"
            android:label="@string/browse_map_full" />
        <activity
            android:name=".BrowseMapModesActivity"
            android:label="@string/browse_map_modes" />
        <activity
            android:name=".BrowseMapClickListenerActivity"
            android:label="@string/browse_map_click_listener" />
        <activity
            android:name=".BrowseMapMarkersActivity"
            android:label="@string/browse_map_markers" />
        <activity
            android:name=".BrowseMapThemesActivity"
            android:label="@string/browse_map_themes" />
        <activity
            android:name=".BrowseMapDetailsWindowActivity"
            android:label="@string/browse_map_details_window">
        </activity>

        <meta-data
            android:name="@string/com_sygic_api_key"
            android:value="${sygicApiKey}" />

    </application>

</manifest>
