<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sygic.samples">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher_browsemap_click_listener"
        android:label="@string/browse_map_click_listener"
        android:roundIcon="@mipmap/ic_launcher_browsemap_click_listener_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sygic.Light.DarkActionBar"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".BrowseMapClickListenerActivity"
            android:label="@string/browse_map_click_listener">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="@string/com_sygic_api_key"
            android:value="${sygicApiKey}" />

    </application>

</manifest>
