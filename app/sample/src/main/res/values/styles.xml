<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--todo-->
        <!--<item name="compassStyle">@style/TestCompassStyle</item>-->
        <!--<item name="positionLockFabStyle">@style/TestPositionLockFabStyle</item>-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--todo-->
    <style name="TestCompassStyle">
        <!--<item name="android:background">@color/black</item>-->
        <item name="android:background">@drawable/ic_map_pin</item>
    </style>

    <style name="TestPositionLockFabStyle" parent="Widget.Design.FloatingActionButton">
        <item name="android:tint">@color/white</item>
        <item name="android:backgroundTint">@color/black</item>
    </style>

</resources>
